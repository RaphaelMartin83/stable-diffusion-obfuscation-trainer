# ==============================================================
# requirements_mock.txt — CPU-only, mock feedback loop
# ==============================================================
#
# Use this on any machine (no GPU required, no diffusers).
#
# Create venv and install:
#
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements_mock.txt \
#               --extra-index-url https://download.pytorch.org/whl/cpu
#
# The --extra-index-url pulls the smaller CPU-only torch wheel (~200 MB)
# instead of the default fat CUDA wheel (~2 GB) from PyPI.
# ==============================================================

torch>=2.12.0
torchvision>=0.27.0
numpy>=2.2.4
pillow>=11.1.0
scikit-image>=0.26.0
scipy>=1.17.1
pyyaml>=6.0.2
tqdm>=4.67.1
